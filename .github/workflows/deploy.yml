name: Deploy CI
on:
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[skip ci]')"
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Deploy to Vercel Action
        uses: BetaHuhn/deploy-to-vercel-action@v1
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERCEL_TOKEN: 'ExWPNFZYHLILtLK4E8M9Ft0y' # Required ExWPNFZYHLILtLK4E8M9Ft0y'
          VERCEL_ORG_ID:  'mateusz.w.twardy@gmail.com'
          VERCEL_PROJECT_ID: 'prj_TvGs6R1u1WugcFSqmgVdHn7mIG6a'
